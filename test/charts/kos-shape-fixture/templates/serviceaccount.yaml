{{- if or (eq .Values.profile "rbac") (eq .Values.profile "stateful") }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}
  labels:
    {{- include "kos-fixture.labels" . | nindent 4 }}
{{- end }}
